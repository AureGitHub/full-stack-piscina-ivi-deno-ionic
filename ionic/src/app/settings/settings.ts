import  {menuItems} from './menu'
import  {message} from './message'



export const settings = {

  Title: 'PISCINA-IVI', //para mostar en pantalla
  Title_color: 'navy',
  app: '',         // pra acceder al server (no se si lo voy a usar)
  menuItems,
  message,
  table : {
    pageSize: 10
  }

  

}

